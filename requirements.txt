# PyPI-only runtime dependencies.
# NumPy/OpenCV are installed through apt in the ROS Humble Docker image so
# cv_bridge remains ABI-compatible with the system Python packages.
mavsdk==3.15.3
PyYAML>=6.0